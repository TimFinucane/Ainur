package algorithm.heuristics;

import common.graph.Graph;
import common.graph.Node;
import common.schedule.Schedule;

import java.util.List;

/**
 * Class is a stub to use in place of actual LowerBound algorithms until official once have been made.
 */
public class LowerBoundStub implements LowerBound {

    /**
     * @param graph
     * @param schedule
     * @param nodesToVisit
     * @return
     */
    //TODO - implementation
    public int estimate(Graph graph, Schedule schedule, List<Node> nodesToVisit) {
        return 0;
    }

    /**
     * @param graph
     * @param schedule
     * @return
     */
    //TODO - implementation
    public int estimate(Graph graph, Schedule schedule) {
        return 0;
    }
}
